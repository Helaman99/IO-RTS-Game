class Unit {
    constructor (name, health, canAttack, damage, range, moveSpeed) {
        this.name = name;
        this.health = health;
        this.canAttack = canAttack;
        this.damage = damage;
        this.range = range;
        this.moveSpeed = moveSpeed;
    }
}